In order to compile the observer.c file type in the following command:
"gcc observer.c"

After compiling in order to run the executable type in the following command:
"./a.out"

To run part C type in the following command:
"./a.out -s"

To run part D type in the following command:
"./a.out -l 10 600"